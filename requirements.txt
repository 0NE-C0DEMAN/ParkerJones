streamlit>=1.30
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
openpyxl>=3.1
pyjwt>=2.8
passlib[bcrypt]>=1.7
bcrypt>=4.0
pyyaml>=6.0
email-validator>=2.0
# Google Sheets backend (optional — only used when FOUNDRY_DB_BACKEND=sheets)
gspread>=6.0
google-auth>=2.27
# Turso/libSQL backend (optional — only used when FOUNDRY_DB_BACKEND=turso)
libsql>=0.1.11
