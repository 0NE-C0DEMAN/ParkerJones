fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
openpyxl>=3.1
pyjwt>=2.8
passlib[bcrypt]>=1.7
bcrypt>=4.0
pyyaml>=6.0
email-validator>=2.0
# Streamlit is dev-only (local `streamlit run app.py`). HF Spaces serves
# everything through FastAPI/uvicorn directly — Streamlit is not invoked
# in the production container.
streamlit>=1.30
