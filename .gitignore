# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# App runtime data (local only)
foundry.db
files/

# Streamlit secrets — never commit real keys
.streamlit/secrets.toml

# User invitation list — contains real emails + JWT secret
users.yaml

# Local / IDE
.cursor/
.claude/
*.log

# OS
.DS_Store
Thumbs.db

# Local sample uploads / binaries (not app source)
/*.pdf
/*.zip
samples/

# Tooling outputs
tools/_*.txt
